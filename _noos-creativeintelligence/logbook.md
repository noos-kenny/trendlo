# Logbook

One line per material change to this repo. Newest first. Format: `YYYY-MM-DD — [who] — what changed and why`.

- 2026-08-17 — Kenny/Claude — **Release 1.1** to `claude-configs/creative-intelligence/` on Kenny's explicit request: everything from the 2026-08-14 phase-1 and phase-2 entries below, in one release. Client mirrors pick it up via `noos-client-setup` / mirror refresh.
- 2026-08-14 — Kenny/Claude — **Phase 2 spec files added** (Kenny's decisions 2026-08-14): `testing/canva_creative_system.md` (four folders as state machine, assembly rules, folder 4 = training data / refusal-list mechanism), `testing/hypothesis_ledger_and_priors_gate.md` + `testing/templates/hypothesis_ledger.md` (ledger format, ad↔hypothesis join via Ad names column, priors gate a–d, write rights to `noos-client-evaluation` only), `testing/image_briefing.md` (negative space per template, background/copy decoupling — 18 variants ≈ 3–6 images), `testing/templates/production_runbook.md` (one source per client for facts; spec always read fresh). `test_process.md` wired to the gate, the ledger and the `Reports/` snapshot convention (`YYMMDD-creative-report-BRANDNAME.md`). NOTE: `Reports/` and `Strategy/hypothesis-ledger.md` also require a knowledge-tree spec version in `claude-configs` before setup/configupdate create them in client trees.
- 2026-08-14 — Kenny/Claude — **Taxonomy resolved: 15 triggers / 5 clusters is canon** (Kenny's decision, from Pontus' improvement doc 2026-08-13). `method/02` gains Cluster 5 "Urgency & Value" (Scarcity/Urgency, Reciprocity, Endowment Effect); 12→15 updated in `method/00`, `method/06`, `learnings/by_trigger/README.md`; `method/03` places the three new triggers and states the confirmed homes: Loss Aversion = TOF, Contrast & Anchoring = BOF, Commitment & Consistency = Retargeting/BOF. `testing/naming_conventions.md`: the menu principle written in as a bearing rule, `bar:` made optional (omitted by default — barrier derives from `con:`), trig picklist corrected to the 15 canonical slugs (`mirror`/`objection`/`listicle` leave it — hooks, not triggers), formal deprecation table (`ang:`→`trig:`, `mall:`→`tmpl:`, `na`, ratio-in-`fmt:`), campaign/ad set cross-reference to `meta-creative-execution`. NOTE: the three new trigger sections must be reconciled against `04_15_triggers_library.md` (methodology library — not in this repo); the sheet's trig picklist (18) must be corrected by Pontus.
- 2026-08-13 — Pontus/Claude — `testing/naming_conventions.md` updated to **v2** of the NOOS Naming Convention sheet, which fixes the prefix collisions in v1: persona `p:` → `pers:`, barrier `b:` → `bar:`, brand `b:` → `br:`, angle `ang:` → trigger `trig:`, plus `tmpl:` added for template. The ad level now carries the full strategy chain (persona → barrier → trigger → concept). Added value rules, token-by-token parser logic (never split naively on `-`), per-level schemas incl. legacy variants, and the decision that `lang:`/`ratio:`/`hyp:` stay client custom fields. `testing/templates/creative_brief.md` ad-name line updated to match.
- 2026-08-07 — Kenny/Claude — Roles clarified: THIS repo is the development environment and master copy; `claude-configs/creative-intelligence/` is the released copy that clients mirror from. `VERSION.md` (v1.0) added with the release workflow. Earlier deprecation notice reverted — it was a misunderstanding.
- 2026-08-04 — Kenny/Claude — `testing/naming_conventions.md` rewritten to the prefixed-attributes model ("Winberg", May 2026) per the NOOS Naming Convention sheet: three levels (Campaign / Ad Set / Ad), empty fields skipped, picklists in the sheet as source of truth. Templates updated to match.
- 2026-08-03 — Kenny/Claude — Initial structure created: `method/` (00–10, migrated from the local NOOS Creative Intelligence knowledge base), `testing/` (process, naming conventions, templates), `learnings/` (empty structure). `06_fashion_retail_playbook.md` removed from the framework; replaced by the general `06_vertical_adaptations.md`.
